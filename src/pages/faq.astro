---
import Layout from '../layouts/Layout.astro';

import faqItems from '../components/FaqItems.json';
---

<Layout
	title="Preguntas Frecuentes"
	description="Descubre las respuestas a tus preguntas sobre instalación y mantenimiento de aires acondicionados split. Como profesional independiente en climatización, proporciono soluciones y consejos para asegurar el rendimiento ideal de tu equipo. ¡Explora nuestras preguntas frecuentes y obtén la información que necesitas para mantener tu ambiente fresco y confortable!">
	<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
		<h1 class="text-3xl font-bold mb-8">Preguntas Frecuentes</h1>
		<div class="grid gap-6">
			{
				faqItems.map((item, index) => (
					<div key={index} class="bg-gray-100 p-6 rounded-lg shadow-md">
						<h2 class="text-xl font-semibold mb-2">{item.question}</h2>
						<p class="text-gray-700">{item.answer}</p>
					</div>
				))
			}
		</div>
	</div>
</Layout>
